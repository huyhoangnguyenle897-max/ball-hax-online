# Ball Hax Online v2
Node.js + WebSocket multiplayer. Max 16 players. Teams are automatically balanced on join and re-picked for every round after a goal.

Render:
- Runtime: Node
- Build Command: npm install
- Start Command: npm start
- Instance: Free for testing

The server listens on 0.0.0.0 and uses process.env.PORT.
